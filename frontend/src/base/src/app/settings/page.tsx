import SettingsPage from "./setting";
import "./style.scss";

export const metadata = {
	title: "Settings",
};

const Settings: React.FC = () => {
	return <SettingsPage />;
};
export default Settings;
